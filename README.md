## Unit Testing week II
## Installation
1. Clone the repository
2. Run `npm install` to install dependencies

## How to Run Tests
Run `npm test` to run Mocha tests from root folder

## How to run extra test program
Run `node src/main.js` from root folder
